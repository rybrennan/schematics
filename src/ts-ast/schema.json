{
  "$schema": "http://json-schema.org/schema",
  "id": "my-collection-ts-ast",
  "title": "Does a whole bunch",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The file which we are changing.",
      "x-prompt": "Enter the destination file path for the wold changing:"
    }
  },
  "required": ["path"]
}